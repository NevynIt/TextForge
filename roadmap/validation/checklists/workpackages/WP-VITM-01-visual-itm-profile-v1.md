# WP-VITM-01 — Visual ITM profile v1

## Gate

Further definition is required before implementation. Produce a concrete Visual ITM v1 profile/spec and examples before coding.

## Checklist

- [x] Visual ITM v1 profile document exists.
- [x] Profile covers nodes, edges, hierarchy, labels, types/classes/tags, styles, source provenance, view/viewpoint provenance, layout hints, renderer hints, and diagnostics.
- [x] Profile explicitly excludes visual editing operations, BPMN-specific semantics, advanced Sigma metrics, automated loss-profile machinery, declared view-delta consumption, and live bidirectional source/visual mirroring from the first slice.
- [x] Extension discipline is documented as guidance, not hard-coded enforcement.
- [x] Example Visual ITM graph and tree/mindmap fixtures exist or are specified.
