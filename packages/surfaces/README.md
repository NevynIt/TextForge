# @textforge/surfaces

Surface registry, session hosting, and open-with routing for TextForge.

Phase 3.4 keeps badge ownership in `@textforge/workspace` and `@textforge/ui`, but projects the resulting resource badge tokens through surface sessions, tabs, and common surface chrome so active-resource orientation stays consistent.
