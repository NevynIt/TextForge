# @textforge/security-profile

Browser-envelope and dependency-boundary checks for TextForge.

Phase 3.4 adds visual-identity boundary coverage so the badge/readability pass stays local and deterministic: no remote icon fetches, no remote badge images, no user-provided icon identity, and no filesystem-derived identity assumptions.
