import type { GeneratedResourceDescriptor } from './types.js';

export declare function createGeneratedResourceDescriptor(
  input: Partial<GeneratedResourceDescriptor> & { readonly path: string },
): GeneratedResourceDescriptor;
