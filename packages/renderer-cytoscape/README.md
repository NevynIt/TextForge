# @textforge/renderer-cytoscape

`@textforge/renderer-cytoscape` provides the Cytoscape runtime surface for Visual ITM graph documents.

It consumes validated in-memory Visual ITM graph objects and exposes a package-owned `.itm` surface contribution that resolves ITM targets through the public `@textforge/itm` APIs before mounting Cytoscape.
