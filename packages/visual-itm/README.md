# @textforge/visual-itm

`@textforge/visual-itm` defines the Visual ITM v1 contract used by resolver and renderer-facing code.

It owns:

- Visual ITM v1 document helpers
- provenance and diagnostic helpers
- lightweight profile validation
- fixture documents that anchor the V19a contract

It does not parse ITM source or mount renderer surfaces.
