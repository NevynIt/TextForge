# @textforge/renderer-sigma

`@textforge/renderer-sigma` provides the Sigma/Graphology runtime surface for dense Visual ITM graph documents.

It consumes validated in-memory Visual ITM graph data and exposes a package-owned `.itm` surface contribution that resolves ITM targets through the public `@textforge/itm` APIs before mounting Sigma.
