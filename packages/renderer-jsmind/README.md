# @textforge/renderer-jsmind

`@textforge/renderer-jsmind` provides the jsMind runtime surface for Visual ITM tree and mindmap documents.

It consumes validated in-memory Visual ITM tree data and exposes a package-owned `.itm` surface contribution that resolves ITM targets through the public `@textforge/itm` APIs before mounting jsMind.
