# Specifications

This folder contains preserved specification notes and format references from the previous TextForge implementation.

Sources include the legacy bundled spec documents under `src/resources/docs/specs/`.
